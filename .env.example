# Copy to .env and replace every value before starting Docker Compose.
MYSQL_ROOT_PASSWORD=replace-with-a-long-unique-root-password
MYSQL_PASSWORD=replace-with-a-long-unique-app-password
# Base64-encoded 256-bit key used to sign JWTs.
APP_JWT_SECRET=replace-with-a-base64-encoded-256-bit-secret
